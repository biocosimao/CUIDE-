
import os from flask import Flask from extensions import db def create_app(): app = Flask(__name__) app.config["SECRET_KEY"] = os.environ.get("SECRET_KEY", "dev-cuide-mais-secret") database_url = os.environ.get("DATABASE_URL") if database_url: if database_url.startswith("postgres://"): database_url = database_url.replace("postgres://", "postgresql://", 1) app.config["SQLALCHEMY_DATABASE_URI"] = database_url else: basedir = os.path.abspath(os.path.dirname(__file__)) app.config["SQLALCHEMY_DATABASE_URI"] = "sqlite:///" + os.path.join(basedir, "cuide.db") app.config["SQLALCHEMY_TRACK_MODIFICATIONS"] = False db.init_app(app) from blueprints.paciente.routes import paciente_bp from blueprints.dashboard.routes import dashboard_bp from blueprints.farmacia.routes import farmacia_bp app.register_blueprint(paciente_bp) app.register_blueprint(dashboard_bp, url_prefix="/dashboard") app.register_blueprint(farmacia_bp, url_prefix="/farmacia") with app.app_context(): import models db.create_all()

